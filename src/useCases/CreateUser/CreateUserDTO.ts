// O DTO vai fazer a comunicação, para fazer o formado de dados que vai ser tranferidoa a uma camada a outra 

export interface ICreateUserRequestDTO{
    name:string,
    email:string,
    password:string,

}
// =========================================